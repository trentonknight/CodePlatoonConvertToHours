function toHours (minutes) {
  return minutes / 60
}

console.log(toHours(45))
console.log(toHours(75))
console.log(toHours(125))

// toHours(45) => 0.75
// toHours(75) => 1.25
// toHours(125) => 2.083
